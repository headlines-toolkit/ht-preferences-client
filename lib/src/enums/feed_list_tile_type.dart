/// {@template feed_list_tile_type}
/// Represents the type of tile used in the news feed list.
/// {@endtemplate}
enum FeedListTileType {
  /// Display the image at the top of the tile.
  imageTop,

  /// Display the image on the start of the tile.
  imageStart,

  /// Display only text in the tile.
  textOnly,
}
