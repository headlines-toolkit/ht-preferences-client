/// {@template app_theme_mode}
/// Represents the app's theme mode.
/// {@endtemplate}
enum AppThemeMode {
  /// Use the system's theme mode.
  system,

  /// Use the light theme mode.
  light,

  /// Use the dark theme mode.
  dark,
}
